/**
 * Russian user-facing strings for the Telegram bot.
 *
 * Edit copy in `messages/*.js` — handlers and keyboards import from here.
 *
 * Callback protocol (CB_* tokens, `filesTreeFolderCallback`) lives in
 * `callbackData.js`. Scheduler presets live in `botSettings.js`.
 */

export * from "./messages/index.js";
